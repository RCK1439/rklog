# rklog examples

## Compilation

- Compiling the examples are as simple as the following:

```bash
make <example>
```

## Running

Running is as easy as

```bash
./<example>_logger_example
```

where `example` is:
- `basic`
- `custom`
- `file`
- `custom_file`
